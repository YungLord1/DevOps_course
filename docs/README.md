#Advanced CI/CD: Jenkins

Доброго времени суток, уважаемый проверяющий! Пробегусь без воды по тому что я сделал в этой домашке:

1. Вынес логику некоторых стейджей в SharedLibrary для более чистого кода в Jenkinsfile. Также сделал ConditionalStage для триггеров, чтобы по каждому триггеру проходили свои стейджи.

2. Настроил параллельные стейджи, сохранение их в артефакты, а также отображение графиков на страницы пайплайна с плагином Warnings.

    #1 Linter(Python) оставил с прошлой дз, линтер flake8
    #2 Linter(Docker) добавил хадулинт
    #3 Unit tests оставил с прошлой дз
    #4 SAST (bandit) Добавил SAST, отчет сохраняется в артефакт, скриншот с ошибками прикладываю(пайп был желтый, ошибки пофиксил. Ошибки были связаны с уязвимостью для XXE-атак, из-за использования xml.etree.ElementTree, заменил ее на более безопасную defusedxml.ElementTree), репорт загрузил в reports
    ![Результат работы Bandit](images/bandit.png)

3. Реализовал по итогу 2 пайплайна:

    #1 Параметризованный пайлпайн, который принимает 2 параметра: IMAGE_TAG И ENVIRONMENT, работа пайплайна описана в файле deploy.jenkinsfile. Также была реализована концепция Blue-green deploy: 
        -Green deploy разворачивается на порту 8001, смоук тесты проверяют работу на этом порту(лежат в папке tests - test_currency_app) и в случае успеха контейнер переходит на основной порт 8000, становясь при этом blue
        ![Логи стейджа Switch Green to Blue](images/bluegreen.png)

    #2 Multibranch pipeline тригеррится от: MR, веток main/master, тегов, и ветки i.chach/homework4, при триггере тэга скипается стейдж deploy to staging и запускается deploy to production, откуда можно будет перейти в параметризованный пайплайн
    ![Deploy тега](images/tagdeploy.png)

4. Немного поправил порты для правильного взаимодействия контейнеров и тестов. В main.py поменял IP c 127.0.0.1 на 0.0.0.0, а тесты перенастроил на localhost:8001

5. Из самого долгого наверное была настройка триггеров для тегов и MR, пытался настроить Конфигурацию пайплайна через Behaviours и build strategies. Также возникали конфликты и MR-ы писали "does not meet criteria" и не искались

6. Еще засорялось место и приходилось чистить его, тк настроил доп 2 Гб free swap space, понял что похоже не нужно оно и пусть висит Warning

7. Добавил TrivyScaner